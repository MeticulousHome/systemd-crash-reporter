[package]
name = "crash-reporter"
version = "0.1.0"
edition = "2024"

[dependencies]
sentry = { version = "0.37.0", features = ["reqwest", "backtrace"] }
openssl = { version = "0.10.72", features = ["vendored"] }
serde_yaml = "0.9.34"
